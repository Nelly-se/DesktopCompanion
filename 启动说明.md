# SpiritDesk 启动说明

> 仓库根目录速查。答辩演示登录注册见 **§3**；详细原理见 `docs/basic/15-登录注册功能详解.md`。  
> **WebView2 / 桌面与网页分工**见 [`docs/basic/webview2/`](docs/basic/webview2/README.md)。

## 环境要求

- Windows 10/11
- [.NET 9 SDK](https://dotnet.microsoft.com/download)
- WebView2 运行时（一般已随 Edge 安装）

首次使用可先构建一次：

```powershell
cd D:\Users\zyx\Desktop\DesktopCompanion
dotnet build SpiritDesk.sln -c Release
```

---

## 一、怎么选启动方式（一张表）

| 你想做什么 | 推荐命令 | 会出现登录/注册？ |
|------------|----------|-------------------|
| 日常玩桌面 + 浮球，省事 | `.\run-shell.ps1` | **否**（直进主页） |
| 答辩演示 **登录 / 注册**（桌面壳） | `.\run-shell-with-auth.ps1` | **是** |
| 只开浏览器网页（含登录） | `.\run-web.ps1` | **是** |
| Shell 连已部署的云端 | 先设 `SPIRITDESK_REMOTE_BASEURL`，再 `.\run-shell.ps1` | **看服务器配置**（一般要登录） |

---

## 二、根目录脚本（推荐）

在 **PowerShell** 里，先 `cd` 到本仓库根目录（含 `SpiritDesk.sln` 的文件夹）。

### 1. 桌面版 · 免登录（默认）

```powershell
.\run-shell.ps1
```

- 自动 Release 构建并启动 `SpiritDesk.Shell`
- 内嵌 WebView2 + 桌面浮球
- 本地 Web 子进程 **关闭** 登录门禁

### 2. 桌面版 · 需要登录注册（答辩用）

```powershell
.\run-shell-with-auth.ps1
```

- 同上，但设置 `SPIRITDESK_REQUIRE_AUTH=1`
- WebView2 会先打开 **`/Login`**
- 演示账号见下文 §4

### 3. 仅网页版

```powershell
.\run-web.ps1
```

- 只跑 `SpiritDesk.Web`，不启动 WPF
- 浏览器地址一般为 **`http://localhost:5160`**（以控制台输出为准）
- Development 环境，**默认要登录**

---

## 三、手写 `dotnet run`（不用脚本时）

### 桌面 Shell · 免登录

```powershell
dotnet run --project .\src\SpiritDesk.Shell\SpiritDesk.Shell.csproj
```

等价于日常 `run-shell.ps1`（**不会**出现登录页）。

### 桌面 Shell · 要登录注册

```powershell
$env:SPIRITDESK_REQUIRE_AUTH = "1"
dotnet run --project .\src\SpiritDesk.Shell\SpiritDesk.Shell.csproj
```

### 仅 Web

```powershell
dotnet run --project .\src\SpiritDesk.Web\SpiritDesk.Web.csproj
```

或指定 Release：

```powershell
dotnet run --project .\src\SpiritDesk.Web\SpiritDesk.Web.csproj -c Release
```

---

## 四、登录 / 注册账号

在 **开启门禁** 时（`run-shell-with-auth.ps1`、`run-web.ps1`、或 Web 的 Development 配置）：

| 用途 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 演示账号 | `spiritdesk` | `spiritdesk` | `appsettings.Development.json`，无需先注册 |
| 自建账号 | 自填 | 至少 6 位 | 打开 `/Register` 注册后，到 `/Login` 登录 |

手动打开页面：

- 登录：`http://localhost:5160/Login`（端口以控制台为准）
- 注册：`http://localhost:5160/Register`

---

## 五、连接云端 Web（可选）

不启动本地 Web，Shell 只当浏览器壳 + 浮球，访问服务器：

```powershell
# 永久设置（新开终端生效）
setx SPIRITDESK_REMOTE_BASEURL "http://你的服务器IP或域名"

# 或仅当前终端
$env:SPIRITDESK_REMOTE_BASEURL = "http://你的服务器IP或域名"
.\run-shell.ps1
```

云端若开启了 `SpiritDesk:Auth:Enabled`，需在 WebView2 里登录；浮球会同步 Cookie。

---

## 六、环境变量速查

| 变量 | 值 | 效果 |
|------|-----|------|
| `SPIRITDESK_REQUIRE_AUTH` | `1` 或 `true` | Shell 本地 Web **保留** 登录门禁 |
| `SPIRITDESK_REMOTE_BASEURL` | 云端根 URL | Shell 不启本地 Web，连远程 |
| `SpiritDesk__Auth__Enabled` | `true` / `false` | 直接开关 Web 门禁（部署常用） |
| `ASPNETCORE_ENVIRONMENT` | `Development` | 合并 `appsettings.Development.json` |

---

## 七、大模型（可选）

聊天要走真实 LLM 时：

1. 复制根目录 `.env.example` 为 `.env`
2. 填写 `ARK_API_KEY` 等
3. 再按上面任一方式启动

未配置时仍可用规则兜底，不影响登录与基础演示。

---

## 八、常见问题

**Q：我跑了 `dotnet run` Shell，为什么没有登录页？**  
A：默认会设 `SpiritDesk__Auth__Enabled=false`。请用 **`.\run-shell-with-auth.ps1`** 或先 `$env:SPIRITDESK_REQUIRE_AUTH="1"`。

**Q：登录页 404 或闪退？**  
A：先 `dotnet build SpiritDesk.sln`，确认 `SpiritDesk.Web` 已成功编译。

**Q：想删掉本地数据重来？**  
A：关闭程序后删除运行目录下的 `data/spiritdesk.db`（Shell 一般在 `src\SpiritDesk.Shell\bin\Debug\net9.0-windows\data\` 或 Release 对应路径）。

**Q：浮球不显示精灵图？**  
A：确认 Web 已启动；查看 `%AppData%\SpiritDesk\companion-bubble.log`。

---

## 九、相关文档

| 文档 | 内容 |
|------|------|
| `docs/basic/15-登录注册功能详解.md` | 登录注册实现与答辩话术 |
| `docs/09-构建运行与部署.md` | 构建、部署、服务器 |
| `README.md` | 项目总览 |

---

## 十、答辩前 30 秒检查清单

- [ ] 要演示登录：用 **`.\run-shell-with-auth.ps1`** 或 **`.\run-web.ps1`**
- [ ] 记住演示账号 **`spiritdesk` / `spiritdesk`**
- [ ] 要演示注册：浏览器进 **`/Register`** → 注册 → 登录
- [ ] 日常演示养成/任务：用 **`.\run-shell.ps1`** 免登录更快
